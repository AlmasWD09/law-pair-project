import { useParams } from "react-router-dom"

const LegalResurcesDetails = () => {
    const {id} = useParams()

  return (
    <div>LegalResurcesDetails page</div>
  )
}

export default LegalResurcesDetails